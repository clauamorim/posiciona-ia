Redeploy da edge function `stripe-checkout`.

Objetivo: garantir que a versão em produção reflita o código atual do repositório.

Ações:
1. Fazer deploy explícito da edge function `stripe-checkout`.
2. Verificar logs para confirmar que a nova instância subiu corretamente.

Não haverá alteração de lógica — apenas atualização da versão em produção.